# Screenshots

Drop PNG screenshots here with these exact names so they render in the main README:

- `dashboard.png` — the popover with the metric cards
- `detail.png` — a hover detail panel (e.g. Battery power flow or Network)
- `settings.png` — the Settings window
- `menubar.png` — the menu bar items / MoodFace

Recommended: capture at 2× (Retina), trim to the window, keep widths consistent.
