# To-do

- [x] Save history in file — history now persists across restarts (saved on quit,
      restored up to 8 days; CPU history included).
- [ ] Screenshots in github page — README section + `docs/screenshots/` ready;
      add PNGs (dashboard/detail/settings/menubar) to render them.
- [x] Wifi strength — Wi-Fi name & RSSI (dBm) show once Location access is granted
      (macOS gates this behind Location Services).
